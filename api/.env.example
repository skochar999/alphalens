# Copy to .env and fill in values

# Path to mf_data/ directory containing parquet files
DATA_DIR=./mf_data

# Port (default 8000)
PORT=8000

# Secret token to protect the /reload endpoint in production
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
RELOAD_SECRET=change_me_in_production
